name: Restaurant Backend CI/CD

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Deploy to Server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e

            # 1. Enter the project directory
            cd /var/www/restaurant/backend

            # 2. Stop PM2 BEFORE pulling new code
            pm2 stop restaurant-backend || true

            # 3. Pull the latest code
            git fetch origin main
            git reset --hard origin/main
            git clean -fd

            # 4. Fresh install & build
            npm ci
            rm -rf dist
            npm run build

            # 5. Delete and restart PM2 (ensures no caching)
            pm2 delete restaurant-backend || true
            pm2 start ecosystem.config.js --env production
            pm2 save
            
            # 6. Verify the process is running
            pm2 list